Radios in Modal need to be require (or have default value upon submission if neither checked).
	-In ModalInstanceCtrl, should probably keep player separate from parent player scope until form is submitted,
	which would help with binding issues (i.e. having to put ng-model-options="{ updateOn: ''}" on everything)

Handle required fields on predraft form.

In Firefox, values in predraft form don't update on blur (i.e. when you just lcik the up/down arrows and then click away).

Progress bar to show total $ and user $ remaining in draft (+ other cool stuff):
	http://angular-ui.github.io/bootstrap/
	https://github.com/angular-ui/bootstrap/tree/master/template

Add ability to undo previous pick.

Move repeated functions to services.
	
In Prod, test if multiple connection work at the same time.